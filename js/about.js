/**
 * about.js — Inicializar navbar na página "Sobre".
 */
import { setNavActive, initNavbarToggle } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
  setNavActive();
  initNavbarToggle();
});
